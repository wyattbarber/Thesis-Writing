Table \ref{tab:sample-count} shows the number samples collected for each background and distance combination. Each test
was performed for the same 2-minute period, however the image processing was not implemented to be real-time and the 
update rate varies somewhat. As a result, the number of samples collected in each 2-minute period varies.

\begin{table}[H]
    \centering
    \caption{Number of sample images collected for each test. Variation between tests is due to non-real-time
        performance of the image processing program, which resulted in variation in the update rate.}
    \label{tab:sample-count}
    \begin{tabular}{|c|c||c|}
        \hline
        Case Number & Distance to Object [mm] & Number of Samples \\
        \hline
        1 & 500 & 9  \\
        1 & 1000 & 10  \\
        1 & 2000 & 11  \\
        2 & 500 & 11 \\
        2 & 1000 & 11 \\
        2 & 2000 & 12 \\
        \hline
    \end{tabular}
\end{table}


\subsection{Perch Detection Success Rate}

For each test trial in table \ref{tab:vision-test-cases}, table \ref{tab:success-rates} shows
the percentage of samples in which the object was correctly detected. Table \ref{tab:success-rates-dist}
shows the same data, but with success rates reported for each distance between camera and object that 
was tested.

\begin{table}[H]
    \centering
    \caption{Detection success rate for each test scenario in table \ref{tab:vision-test-cases}.}
    \label{tab:success-rates}
    \begin{tabular}{|c||c|}
        \hline
        Case Number & Success Rate [\%] \\
        \hline
        1 & 100.0  \\
        2 & 63.6 \\
        \hline
    \end{tabular}
\end{table}

\begin{table}[H]
    \centering
    \caption{Detection success rate for each test scenario in table \ref{tab:vision-test-cases}, 
    separated by distance to the object.}
    \label{tab:success-rates-dist}
    \begin{tabular}{|c|c||c|}
        \hline
        Case Number & Distance to Object [mm] & Success Rate [\%] \\
        \hline
        1 & 500 & 100.0  \\
        1 & 1000 & 100.0  \\
        1 & 2000 & 100.0  \\
        2 & 500 & 100.0 \\
        2 & 1000 & 100.0 \\
        2 & 2000 & 0.0 \\
        \hline
    \end{tabular}
\end{table}
